services:
  - type: web
    name: ai-resume-job-analyzer
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
      - key: GOOGLE_API_KEY
        sync: false
